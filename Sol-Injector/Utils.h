// will hold all of the helper functions that do not fit else where
#pragma once
#include <iostream>

namespace Utils {
	void clearAndIgnoreInput();
	void waitForKey();
	void copyDiscordLink(const std::string& text);
}